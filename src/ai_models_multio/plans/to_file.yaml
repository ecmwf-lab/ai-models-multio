plans:
- actions:
  - type : encode
    format : grib
    # template : grib_template.grib
    grid-type : n320
  - sinks:
    - append: true
      # path: testWriteOutput.grib
      per-server: false
      type: file
    type: sink
  name: 'output-to-file-plan'
